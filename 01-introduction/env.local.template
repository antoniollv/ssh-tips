# AWS Configuration for S3 Terraform Backend
# Copy this file to env.local and configure your values
# env.local is in .gitignore and will not be committed

# AWS Credentials (optional if using AWS CLI profile)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# AWS Region for S3 bucket (eu-west-1 = Ireland)
AWS_REGION=eu-west-1

# S3 Bucket name for Terraform state (must be globally unique)
TF_STATE_BUCKET=ssh-tips-terraform-state

# AWS CLI Profile (optional, uncomment if using named profiles)
# AWS_PROFILE=default

# GitHub repository information (for IAM role creation)
GITHUB_ORG=antoniollv
GITHUB_REPO=ssh-tips

# IAM Role name for GitHub Actions (optional, uses default if empty)
IAM_ROLE_NAME=GitHubActionsRole-ssh-tips
IAM_POLICY_NAME=GitHubActionsPolicy-ssh-tips
