# Case 2 - Database SSH Tunnel Configuration
# Fill this file with values from AWS Secrets Manager and rename to env.local

# Retrieve values with:
# aws secretsmanager get-secret-value \
#   --secret-id ssh-tips/case02-database-credentials \
#   --region eu-west-1 \
#   --query SecretString --output text | jq

BASTION_PUBLIC_IP=""
RDS_ADDRESS=""
DB_NAME=""
DB_USERNAME=""
DB_PASSWORD=""
DB_PORT="3306"
SSH_USER="ec2-user"
SSH_KEY_PATH="$HOME/.ssh/id_rsa"
